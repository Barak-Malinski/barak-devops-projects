apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: barakm7/gitaction:latest
      imagePullPolicy: Always
      ports:
        - containerPort: 3000
